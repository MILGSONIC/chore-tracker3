{
  "name": "miles-sparrow-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "preview": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^4.9.0"
  }
}
